'use strict'

class Set {

  function all () {
    return Promise.resolve([])
  }

  function byId (id) {
    return Promise.resolve()
  }

  function query (sort, search) {
    return Promise.resolve([])
  }

}

module.exports = Set
